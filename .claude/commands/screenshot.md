Update the README screenshot.

Steps:
1. Make sure SimpleText is running (launch it if needed via `open build/SimpleText.app`)
2. Run `bash .claude/screenshot.sh` to capture the window into `docs/screenshot.png`
3. Read `docs/screenshot.png` to verify it looks correct (app visible, not terminal)
4. Commit with `git add docs/screenshot.png` and an appropriate commit message
